
function search_f_s(search,outp){
      search=search.toUpperCase();
    var  filename='no',discription='';

if (search=='AB')
{
    filename='ab_ac_ad.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: ' + search;
}
if (search=='AC')
{
    filename='ab_ac_ad.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: AC';
}
if (search=='AD')
{
    filename='ab_ac_ad.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: AB, AC, AD';
}
if (search=='AE')
{
    filename='ae.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: AE';
}
if (search=='AF')
{
    filename='af.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: AF';
}
if (search=='AG')
{
    filename='ag.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: AG';
}
if (search=='AH')
{
    filename='ah.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: AH';
}
if (search=='AK')
{
    filename='ak.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: AK';
}
if (search=='AL')
{
    filename='al_am.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: AL';
}
if (search=='AM')
{
    filename='al_am.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: AM';
}

if (search=='AP')
{
    filename='ap.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: AP';
}
if (search=='AR')
{
    filename='ar_ax_az.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: AR';
}


if (search=='AX')
{
    filename='ar_ax_az.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark:  AX, AZ';
}
if (search=='AZ')
{
    filename='ar_ax_az.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark:  AX, AZ';
}
if (search=='BA')
{
    filename='ba.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: BA';
}
if (search=='BE')
{
    filename='be_bl.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: BE, BL';
}
if (search=='BL')
{
    filename='be_bl.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: BE, BL';
}
if (search=='BM')
{
    filename='bm_bn.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: BM, BN';
}
if (search=='BN')
{
    filename='bm_bn.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: BM, BN';
}
if (search=='BO')
{
    filename='bo.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: BO';
}
if (search=='BP')
{
    filename='bp_br.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: BP, BR';
}
if (search=='BR')
{
    filename='bp_br.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: BP, BR';
}
if (search=='CA')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='CB')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='CL')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='CN')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='CO')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
 if (search=='CP')
 {
     filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
     discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
 }
if (search=='CV')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='CY')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='DA')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='DB')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='DE')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='DF')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='DH')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='DL')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='DM')
{
    filename='ca_cb_cl_cn_co_cv_cy_da_db_de_df_dh_dl_dm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: CA, CB, CL, CN, CO, CV, CY, DA, DB, DE, DF, DH, DL, DM';
}
if (search=='DP')
{
    filename='dp.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: DP';
}
if (search=='DX')
{
    filename='dx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: DX';
}
if (search=='EA')
{
    filename='ea.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EA, EB';
}
if (search=='EB')
{
    filename='eb.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EA, EB';
}
if (search=='ED')
{
    filename='ed.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: ED';
}
 
if (search=='EF')
{
    filename='ef.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EF';
}
if (search=='EK')
{
    filename='ek.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EK';
}
if (search=='EL')
{
    filename='el_em.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EL,EM';
}
if (search=='EM')
{
    filename='el_em.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EL,EM';
}
if (search=='EN')
{
    filename='en.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EN';
}
if (search=='EO')
{
    filename='eo_er_es_et.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EO, ER, ES, ET';
}
if (search=='ER')
{
    filename='eo_er_es_et.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EO, ER, ES, ET';
}
if (search=='ES')
{
    filename='eo_er_es_et.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EO, ER, ES, ET';
}
if (search=='ET')
{
    filename='eo_er_es_et.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EO, ER, ES, ET';
}
if (search=='EU')
{
    filename='eu.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EU';
}
if (search=='EX')
{
    filename='ex_ez_fa_fe.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EX, FA, FE';
}
if (search=='EZ')
{
    filename='ex_ez_fa_fe.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EZ';
}
if (search=='FA')
{
    filename='ex_ez_fa_fe.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EX, FA, FE';
}
if (search=='FE')
{
    filename='ex_ez_fa_fe.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: EX, FA, FE';
}
if (search=='FK')
{
    filename='fk.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: FK';
}
if (search=='FL')
{
    filename='fl_fm_fq_fu.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: FL, FM, FQ, FU';
}
if (search=='FM')
{
    filename='fl_fm_fq_fu.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: FL, FM, FQ, FU';
}
if (search=='FQ')
{
    filename='fl_fm_fq_fu.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: FL, FM, FQ, FU';
}
if (search=='FU')
{
    filename='fl_fm_fq_fu.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: FL, FM, FQ, FU';
}
if (search=='GA')
{
    filename='ga_gb_ge_gl_gx_ha_he.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: GA, GB, GE, GL, GX, HA, HE, HL, HN, HS';
}
if (search=='GB')
{
    filename='ga_gb_ge_gl_gx_ha_he.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: GA, GB, GE, GL, GX, HA, HE, HL, HN, HS';
}
if (search=='GE')
{
    filename='ga_gb_ge_gl_gx_ha_he.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: GA, GB, GE, GL, GX, HA, HE, HL, HN, HS';
}
if (search=='GL')
{
    filename='ga_gb_ge_gl_gx_ha_he.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: GA, GB, GE, GL, GX, HA, HE, HL, HN, HS';
}
if (search=='GX')
{
    filename='ga_gb_ge_gl_gx_ha_he.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: GA, GB, GE, GL, GX, HA, HE, HL, HN, HS';
}
if (search=='HA')
{
    filename='ga_gb_ge_gl_gx_ha_he.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: GA, GB, GE, GL, GX, HA, HE, HL, HN, HS';
}
if (search=='HE')
{
    filename='ga_gb_ge_gl_gx_ha_he.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: GA, GB, GE, GL, GX, HA, HE, HL, HN, HS';
}
if (search=='HL')
{
    filename='hl_hn.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: GA, GB, GE, GL, GX, HA, HE, HL, HN, HS';
}
if (search=='HN')
{
    filename='hl_hn.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: GA, GB, GE, GL, GX, HA, HE, HL, HN, HS';
}
if (search=='HS')
{
    filename='hs.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: HS';
}
if (search=='HX')
{
    filename='hx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: HX';
}
if (search=='IA')
{
    filename='ia.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: IA';
}
if (search=='IB')
{
    filename='ib.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: IB';
}
if (search=='IC')
{
    filename='ic.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: IC';
}
if (search=='ID')
{
    filename='id.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: ID';
}
if (search=='IL')
{
    filename='il_io_ip_iq.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: IL, IO, IP, IQ';
}
if (search=='IO')
{
    filename='il_io_ip_iq.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: IL, IO, IP, IQ';
}
if (search=='IP')
{
    filename='il_io_ip_iq.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: IL, IO, IP, IQ';
}
if (search=='IQ')
{
    filename='il_io_ip_iq.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: IL, IO, IP, IQ';
}
if (search=='IR')
{
    filename='ir.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: IR';
}
if (search=='IS')
{
    filename='is.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: IS';
}
if (search=='IT')
{
    filename='it.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: IT, IW, IX';
}
if (search=='IW')
{
    filename='iw.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: IT, IW, IX';
}
if (search=='IX')
{
    filename='ix_ma.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: IT, IW, IX';
}
if (search=='MA')
{
    filename='ix_ma.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: MA';
}
if (search=='MB')
{
    filename='mb_mc.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: MB, MC';
}
if (search=='MC')
{
    filename='mb_mc.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: MB, MC';
}
if (search=='MD')
{
    filename='md_me_mg.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: MD, ME, MG';
}
if (search=='ME')
{
    filename='md_me_mg.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: MD, ME, MG';
}
if (search=='MG')
{
    filename='md_me_mg.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: MD, ME, MG';
}
if (search=='ML')
{
    filename='ml.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: ML';
}
if (search=='MN')
{
    filename='mn.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: MN';
}
if (search=='MT')
{
    filename='mt.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: MT';
}
if (search=='MW')
{
    filename='mw.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: MW';
}
if (search=='MY')
{
    filename='my.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: MY';
}
if (search=='NA')
{
    filename='na.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: ND, NE, NG, NK';
}

if (search=='ND')
{
    filename='nd_ne_ng_nk.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: ND, NE, NG, NK';
}
if (search=='NE')
{
    filename='nd_ne_ng_nk.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: ND, NE, NG, NK';
}

if (search=='NG')
{
    filename='nd_ne_ng_nk.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: ND, NE, NG, NK';
}
if (search=='NK')
{
    filename='nd_ne_ng_nk.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: ND, NE, NG, NK';
}
//if (search=='NM')
//{
//    filename='nm.png';
//    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: NM';
//}
if (search=='NP')
{
    filename='np.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: NP, NT, NW, NX';
}
if (search=='NT')
{
    filename='nt_nw_nx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: NP, NT, NW, NX';
}
if (search=='NW')
{
    filename='nt_nw_nx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: NP, NT, NW, NX';
}
if (search=='NX')
{
    filename='nt_nw_nx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: NP, NT, NW, NX';
}
if (search=='PA')
{
    filename='pa.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: PA';
}
if (search=='PE')
{
    filename='pe_ph_pk.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: PE, PH, PK';
}
if (search=='PH')
{
    filename='pe_ph_pk.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: PE, PH, PK';
}
if (search=='PK')
{
    filename='pe_ph_pk.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: PE, PH, PK';
}
if (search=='PL')
{
    filename='pl_pm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: PL, PM';
}
if (search=='PM')
{
    filename='pl_pm.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: PL, PM';
}
if (search=='PO')
{
    filename='po_ps_pv_pw.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: PO, PS, PV, PW';
}
if (search=='PS')
{
    filename='po_ps_pv_pw.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: PO, PS, PV, PW';
}
if (search=='PV')
{
    filename='po_ps_pv_pw.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: PO, PS, PV, PW';
}
if (search=='PW')
{
    filename='po_ps_pv_pw.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: PO, PS, PV, PW';
}
if (search=='RA')
{
    filename='ra_rl_rs_sn_sx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: RA, RL, RS, SN, SX';
}
if (search=='RL')
{
    filename='ra_rl_rs_sn_sx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: RA, RL, RS, SN, SX';
}
if (search=='RS')
{
    filename='ra_rl_rs_sn_sx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: RA, RL, RS, SN, SX';
}
if (search=='SN')
{
    filename='ra_rl_rs_sn_sx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: RA, RL, RS, SN, SX';
}
if (search=='SX')
{
    filename='ra_rl_rs_sn_sx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: RA, RL, RS, SN, SX';
}
if (search=='TA')
{
    filename='ta_te_tg_tu_ua_ue_ul_un_ut_ux.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: TA, TE, TG, TU, UA, UE, UL, UN, UX';
}
if (search=='TE')
{
    filename='ta_te_tg_tu_ua_ue_ul_un_ut_ux.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: TA, TE, TG, TU, UA, UE, UL, UN, UX';
}
if (search=='TG')
{
    filename='ta_te_tg_tu_ua_ue_ul_un_ut_ux.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: TA, TE, TG, TU, UA, UE, UL, UN, UX';
}
if (search=='TU')
{
    filename='ta_te_tg_tu_ua_ue_ul_un_ut_ux.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: TA, TE, TG, TU, UA, UE, UL, UN, UX';
}
if (search=='UA')
{
    filename='ta_te_tg_tu_ua_ue_ul_un_ut_ux.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: TA, TE, TG, TU, UA, UE, UL, UN, UX';
}
if (search=='UE')
{
    filename='ta_te_tg_tu_ua_ue_ul_un_ut_ux.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: TA, TE, TG, TU, UA, UE, UL, UN, UX';
}
if (search=='UL')
{
    filename='ta_te_tg_tu_ua_ue_ul_un_ut_ux.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: TA, TE, TG, TU, UA, UE, UL, UN, UX';
}
if (search=='UN')
{
    filename='ta_te_tg_tu_ua_ue_ul_un_ut_ux.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: TA, TE, TG, TU, UA, UE, UL, UN, UX';
}
if (search=='UT')
{
    filename='ta_te_tg_tu_ua_ue_ul_un_ut_ux.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: TA, TE, TG, TU, UA, UE, UL, UN, UX';
}
if (search=='UX')
{
    filename='ta_te_tg_tu_ua_ue_ul_un_ut_ux.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: TA, TE, TG, TU, UA, UE, UL, UN, UX';
}
if (search=='VA')
{
    filename='va_vb_vc_vd_ve_vl_vx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: VA, VB, VC, VD, VE, VL, VX';
}
if (search=='VB')
{
    filename='va_vb_vc_vd_ve_vl_vx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: VA, VB, VC, VD, VE, VL, VX';
}
if (search=='VC')
{
    filename='va_vb_vc_vd_ve_vl_vx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: VA, VB, VC, VD, VE, VL, VX';
}
if (search=='VD')
{
    filename='va_vb_vc_vd_ve_vl_vx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: VA, VB, VC, VD, VE, VL, VX';
}
if (search=='VE')
{
    filename='va_vb_vc_vd_ve_vl_vx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: VA, VB, VC, VD, VE, VL, VX';
}
if (search=='VL')
{
    filename='va_vb_vc_vd_ve_vl_vx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: VA, VB, VC, VD, VE, VL, VX';
}
if (search=='VX')
{
    filename='va_vb_vc_vd_ve_vl_vx.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: VA, VB, VC, VD, VE, VL, VX';
}
if (search=='WA')
{
    filename='wa_wk_wl_wp_wx_xa.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: WA, WK, WP, WX';
}
if (search=='WK')
{
    filename='wa_wk_wl_wp_wx_xa.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: WA, WK, WP, WX';
}
if (search=='WL')
{
    filename='wa_wk_wl_wp_wx_xa.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: WA, WK, WP, WX';
}
if (search=='WP')
{
    filename='wa_wk_wl_wp_wx_xa.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: WA, WK, WP, WX';
}
if (search=='WX')
{
    filename='wa_wk_wl_wp_wx_xa.png';
    discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: WA, WK, WP, WX';
}
//if (search=='XA')
//{
 //   filename='wa_wk_wl_wp_wx_xa.png';
 //   discription='Signatur ' + search;  discription2='shelfmark: ' + search; //discription='Signatur ' + search;  discription2='shelfmark: XA';
//}


if(outp=='discription'){
  var lang = navigator.language.split("-");
var current_lang = (lang[0]);
 if(current_lang=='de'){ 
return discription;
 }
else{
    return discription2;
    }


    

}else{
     return filename;
}

}